[Unit]
Description=Qwen Podman Network
After=network.target

[Service]
Type=oneshot
ExecStart=/usr/bin/podman network create qwen-network
ExecStop=/usr/bin/podman network rm qwen-network
RemainAfterExit=yes

[Install]
WantedBy=multi-user.target